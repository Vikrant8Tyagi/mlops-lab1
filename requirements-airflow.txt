# ============================
# Airflow SAFE Dependencies
# ============================

# Core (provider-compatible)
pandas>=2.1.2,<2.2
numpy>=1.26,<2.0

# ORM (Airflow compatible)
sqlalchemy<2.0

# AWS (strict pinning)
boto3>=1.35.16,<1.35.17
botocore>=1.35.16,<1.35.17

# Crypto stack (provider safe)
cryptography>=41.0.5,<44
cffi>=1.9,<2.0

# Airflow Providers
apache-airflow-providers-google
apache-airflow-providers-snowflake

# Utilities
pyyaml==6.0.3
psycopg2-binary==2.9.10
